    //std::string folderpath = "/Users/anirud/Downloads/CSProjects/burdock/entry";
    // std::vector<std::filesystem::path> files;
    // if (std::filesystem::exists(folderpath) && std::filesystem::is_directory(folderpath)) {
    //     for (const auto& f : std::filesystem::directory_iterator(folderpath)) {
    //         if (f.is_regular_file()) {
    //             files.push_back(f.path());
    //         }
    //     }
    // }
    // std::string f = "1231235.cpp";
    // bool started_num = false;
    // std::string num = "";
    // for (unsigned i = f.size() - 1; i != UNSIGNED_MAX; i--) {
    //     char curr = f.at(i);
    //     if (started_num) {
    //         if (isdigit(curr)) {
    //             num = curr + num;
    //         } else {
    //             break;
    //         }
    //     }
    //     if (curr == '.') {
    //         started_num = true;
    //     }
    // }
    // std::cout << std::stoi(num) << std::endl;